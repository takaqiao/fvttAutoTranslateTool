# 这一批 rank 批次已过期，**不要落盘**

生成时间 2026-08-15 05:55，是**本段落盘之前**算出来的整叶字符串。
`apply_translations.py` 是整叶覆盖，直落会把同一批叶子上后来落的改动
（Token→指示物、drop_terms 修正等）静默回滚 —— §8 2026-08-12b 那条教训。

正确做法（第十六轮收口段实做）：删掉这一份 → 重跑探针 → 落新算出来的。

```powershell
python "$P\4-临时脚本\2026-08-15-round16\probes\split_rank.py" `
       --repo "$P\1-Ember汉化插件" --repo "$P\2-Crucible汉化插件" `
       --out-dir "$P\4-临时脚本\2026-08-15-round16\rank-batches"
```

## 重跑结果：**改写 0 叶 + 人裁 0 叶**，这 176 叶上一段已经落完了

佐证（英文闸 `\branks?\b` **带 `re.IGNORECASE`**，两仓 compendium）：

| | 主控任务书里的口径（落盘前） | 现测（落盘后） |
|---|---|---|
| 命中叶（英文有 rank 且有中文） | — | 391 |
| 含「等级」 | 223 | **51** |
| 含「阶位」 | 47 | **221** |

51 = 探针的「全普通名词义不动 33」+「需人看 18」，逐项对得上，没有漏网。

## 探针那 4 条 `⚠ 人裁表失效` 是**已落盘**的信号，不是措辞被人动过

`apply_manual()` 数的是**旧串**，落完之后旧串当然是 0 处。逐叶实测新串都在、次数也对：

| 叶 | 表里的旧串（实测） | 对应新串（实测） |
|---|---|---|
| GM's Guide / Death and Soulbound（孪生 2 包） | 等级晋升 0 · 适当等级的 0 | 阶位晋升 1 · 适当阶位的 1 |
| crucible.rules / Character Mechanics.Talents | `<p>等级</p>` 0 | `<p>阶位</p>` 1 |
| crucible.rules / Adversaries.Overview | 更高等级 0 · 训练等级 0 | 更高阶位 1 · 训练阶位 3 |

所以**没有触发「停下来报告」那条**：库里的措辞没被动过，只是这张表对已落盘的叶子不幂等。
下次重跑前，要么把这 4 行从 `MANUAL` 里摘掉，要么让 `apply_manual` 在新串已就位时静默跳过。
